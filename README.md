# GIT
a)la commande git pull permet de  fusionner toutes les modifications présentes sur le dépôt distant dans le répertoire de travail local alors que la commaénde git fetch permet  à un utilisateur d’extraire tous les fichiers du dépôt distant qui ne sont pas actuellement dans le répertoire de travail local.

b) cest la commande git checkout
